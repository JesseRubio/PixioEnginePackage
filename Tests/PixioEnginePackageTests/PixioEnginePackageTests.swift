    import XCTest
    @testable import PixioEnginePackage

    final class PixioEnginePackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(PixioEnginePackage().text, "Hello, World!")
        }
    }
